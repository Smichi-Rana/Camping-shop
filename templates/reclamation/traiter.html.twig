{% extends 'base.html.twig' %}

{% block title %}Traiter la réclamation #{{ reclamation.id }} - CampingShop{% endblock %}

{% block body %}
    <style>
        .traiter-container {
            max-width: 900px;
            margin: 2rem auto;
            padding: 2rem;
        }

        .btn-back {
            background: white;
            color: #667eea;
            border: 2px solid #667eea;
            padding: 0.8rem 1.5rem;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 600;
            display: inline-block;
            transition: all 0.3s;
            margin-bottom: 2rem;
        }

        .btn-back:hover {
            background: #667eea;
            color: white;
        }

        .traiter-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
            animation: slideUp 0.6s ease;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .traiter-header {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            padding: 2rem;
            text-align: center;
        }

        .traiter-header h1 {
            font-size: 2rem;
            margin: 0;
        }

        .traiter-body {
            padding: 2.5rem;
        }

        .info-box {
            background: #fff3cd;
            border-left: 4px solid #ffc107;
            padding: 1.2rem;
            border-radius: 8px;
            margin-bottom: 2rem;
            color: #856404;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .info-icon {
            font-size: 1.5rem;
        }

        .reclamation-summary {
            background: #f8f9fa;
            padding: 1.5rem;
            border-radius: 10px;
            margin-bottom: 1.5rem;
            border-left: 4px solid #667eea;
        }

        .summary-title {
            font-weight: 600;
            color: #333;
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
            text-transform: uppercase;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .summary-text {
            color: #555;
            line-height: 1.6;
            font-size: 1rem;
        }

        .form-group {
            margin-bottom: 2rem;
        }

        .form-group label {
            display: block;
            font-weight: 600;
            color: #333;
            margin-bottom: 0.8rem;
            font-size: 1.1rem;
        }

        .required::after {
            content: ' *';
            color: #dc3545;
        }

        .form-control {
            width: 100%;
            padding: 1.2rem;
            border: 2px solid #ddd;
            border-radius: 10px;
            font-size: 1rem;
            font-family: inherit;
            resize: vertical;
            min-height: 200px;
            transition: all 0.3s;
            line-height: 1.6;
        }

        .form-control:focus {
            outline: none;
            border-color: #28a745;
            box-shadow: 0 0 0 4px rgba(40, 167, 69, 0.1);
        }

        .form-control::placeholder {
            color: #999;
        }

        .char-counter {
            text-align: right;
            color: #666;
            font-size: 0.9rem;
            margin-top: 0.5rem;
        }

        .btn-submit {
            width: 100%;
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            border: none;
            padding: 1.3rem;
            border-radius: 10px;
            font-size: 1.2rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.8rem;
        }

        .btn-submit:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(40, 167, 69, 0.4);
        }

        .btn-submit:active {
            transform: translateY(-1px);
        }

        .client-info {
            background: #e7f3ff;
            padding: 1.5rem;
            border-radius: 10px;
            margin-bottom: 2rem;
            border-left: 4px solid #0066cc;
        }

        .client-info-title {
            font-weight: 600;
            color: #0066cc;
            margin-bottom: 0.8rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .client-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
        }

        .client-detail-item {
            display: flex;
            flex-direction: column;
            gap: 0.3rem;
        }

        .detail-label {
            font-size: 0.85rem;
            color: #666;
            font-weight: 600;
        }

        .detail-value {
            color: #333;
            font-weight: 500;
        }

        .tips-box {
            background: #d4edda;
            border-left: 4px solid #28a745;
            padding: 1.2rem;
            border-radius: 8px;
            margin-bottom: 1.5rem;
        }

        .tips-title {
            font-weight: 600;
            color: #155724;
            margin-bottom: 0.8rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .tips-list {
            margin: 0;
            padding-left: 1.5rem;
            color: #155724;
        }

        .tips-list li {
            margin-bottom: 0.5rem;
        }

        @media (max-width: 768px) {
            .traiter-header h1 {
                font-size: 1.5rem;
            }

            .traiter-body {
                padding: 1.5rem;
            }
        }
    </style>

    <div class="traiter-container">
        <a href="{{ path('app_reclamation_show', {id: reclamation.id}) }}" class="btn-back">
            ← Retour aux détails
        </a>

        <div class="traiter-card">
            <div class="traiter-header">
                <h1>✍️ Traiter la réclamation #{{ reclamation.id }}</h1>
            </div>

            <div class="traiter-body">
                {# Warning Box #}
                <div class="info-box">
                    <span class="info-icon">⚠️</span>
                    <div>
                        <strong>Attention :</strong> Une fois traitée, la réclamation ne pourra plus être modifiée et le client recevra automatiquement un email de notification avec votre réponse.
                    </div>
                </div>

                {# Client Info #}
                <div class="client-info">
                    <div class="client-info-title">
                        <span>👤</span>
                        <span>Informations du client</span>
                    </div>
                    <div class="client-details">
                        <div class="client-detail-item">
                            <span class="detail-label">Nom</span>
                            <span class="detail-value">{{ reclamation.nomUtilisateur }}</span>
                        </div>
                        <div class="client-detail-item">
                            <span class="detail-label">Email</span>
                            <span class="detail-value">{{ reclamation.emailUtilisateur }}</span>
                        </div>
                        <div class="client-detail-item">
                            <span class="detail-label">Date de création</span>
                            <span class="detail-value">{{ reclamation.dateCreation|date('d/m/Y à H:i') }}</span>
                        </div>
                        <div class="client-detail-item">
                            <span class="detail-label">Catégorie</span>
                            <span class="detail-value">{{ reclamation.categorie|replace({'_': ' '})|title }}</span>
                        </div>
                    </div>
                </div>

                {# Réclamation Details #}
                <div class="reclamation-summary">
                    <div class="summary-title">
                        <span>📋</span>
                        <span>Titre de la réclamation</span>
                    </div>
                    <div class="summary-text">
                        <strong>{{ reclamation.titre }}</strong>
                    </div>
                </div>

                <div class="reclamation-summary">
                    <div class="summary-title">
                        <span>📄</span>
                        <span>Description du problème</span>
                    </div>
                    <div class="summary-text">{{ reclamation.description }}</div>
                </div>

                {# Tips Box #}
                <div class="tips-box">
                    <div class="tips-title">
                        <span>💡</span>
                        <span>Conseils pour une bonne réponse</span>
                    </div>
                    <ul class="tips-list">
                        <li>Commencez par remercier le client pour sa réclamation</li>
                        <li>Reconnaissez le problème et exprimez vos excuses</li>
                        <li>Proposez une solution concrète</li>
                        <li>Soyez professionnel et courtois</li>
                        <li>Terminez par une formule de politesse</li>
                    </ul>
                </div>

                {# Form #}
                <form method="post">
                    <div class="form-group">
                        <label for="reponse" class="required">💬 Votre réponse au client</label>
                        <textarea
                            id="reponse"
                            name="reponse"
                            class="form-control"
                            placeholder="Bonjour [Nom du client],&#10;&#10;Nous vous remercions d'avoir pris le temps de nous contacter concernant...&#10;&#10;Nous comprenons votre frustration et nous vous présentons nos sincères excuses pour...&#10;&#10;Pour résoudre ce problème, nous vous proposons...&#10;&#10;Cordialement,&#10;L'équipe CampingShop"
                            required
                            minlength="50"></textarea>
                        <div class="char-counter">
                            <span id="charCount">0</span> caractères
                        </div>
                    </div>

                    <button type="submit" class="btn-submit">
                        <span>✅</span>
                        <span>Envoyer la réponse et clôturer la réclamation</span>
                    </button>
                </form>
            </div>
        </div>
    </div>

    <script>
        // Compteur de caractères
        document.addEventListener('DOMContentLoaded', function() {
            const textarea = document.getElementById('reponse');
            const charCount = document.getElementById('charCount');

            textarea.addEventListener('input', function() {
                charCount.textContent = this.value.length;
            });
        });
    </script>
{% endblock %}
