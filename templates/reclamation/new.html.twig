{% extends 'base.html.twig' %}

{% block title %}Nouvelle réclamation - CampingShop{% endblock %}

{% block body %}
    <style>
        .reclamation-container {
            max-width: 900px;
            margin: 3rem auto;
            padding: 2rem;
        }

        .page-header {
            text-align: center;
            color: white;
            margin-bottom: 3rem;
        }

        .page-header h1 {
            font-size: 3rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
            margin-bottom: 1rem;
        }

        .page-header p {
            font-size: 1.2rem;
            opacity: 0.95;
        }

        .reclamation-card {
            background: white;
            padding: 3rem;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
            animation: slideUp 0.6s ease;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .form-title {
            font-size: 1.8rem;
            color: #333;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 3px solid #667eea;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group label {
            display: block;
            font-weight: 600;
            color: #333;
            margin-bottom: 0.5rem;
            font-size: 1rem;
        }

        .required::after {
            content: ' *';
            color: #dc3545;
        }

        .form-control {
            width: 100%;
            padding: 0.9rem 1rem;
            border: 2px solid #ddd;
            border-radius: 10px;
            font-size: 1rem;
            transition: all 0.3s;
            font-family: inherit;
        }

        .form-control:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        textarea.form-control {
            resize: vertical;
            min-height: 120px;
        }

        .form-select {
            width: 100%;
            padding: 0.9rem 1rem;
            border: 2px solid #ddd;
            border-radius: 10px;
            font-size: 1rem;
            transition: all 0.3s;
            background: white;
            cursor: pointer;
        }

        .form-select:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .btn-submit {
            width: 100%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 1.2rem;
            border-radius: 10px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            margin-top: 1rem;
        }

        .btn-submit:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
        }

        .btn-back {
            background: white;
            color: #667eea;
            border: 2px solid #667eea;
            padding: 0.9rem 2rem;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 600;
            display: inline-block;
            transition: all 0.3s;
            margin-bottom: 2rem;
        }

        .btn-back:hover {
            background: #667eea;
            color: white;
        }

        .info-box {
            background: #e7f3ff;
            border-left: 4px solid #667eea;
            padding: 1.5rem;
            border-radius: 10px;
            margin-bottom: 2rem;
        }

        .info-box h3 {
            color: #667eea;
            margin-bottom: 0.5rem;
            font-size: 1.1rem;
        }

        .info-box p {
            color: #555;
            margin: 0;
            line-height: 1.6;
        }

        .form-errors {
            background: #f8d7da;
            color: #721c24;
            padding: 0.75rem;
            border-radius: 8px;
            margin-top: 0.5rem;
            font-size: 0.9rem;
        }

        @media (max-width: 768px) {
            .page-header h1 {
                font-size: 2rem;
            }

            .reclamation-card {
                padding: 1.5rem;
            }
        }
    </style>

    <div class="reclamation-container">
        <a href="{{ path('app_reclamation_index') }}" class="btn-back">
            ← Retour à mes réclamations
        </a>

        <div class="page-header">
            <h1>📝 Nouvelle Réclamation</h1>
            <p>Décrivez votre problème et nous vous répondrons dans les plus brefs délais</p>
        </div>

        <div class="reclamation-card">
            <h2 class="form-title">
                <span>📋</span>
                Formulaire de réclamation
            </h2>

            <div class="info-box">
                <h3>ℹ️ Informations importantes</h3>
                <p>
                    Nous nous engageons à traiter votre réclamation dans un délai de 48h maximum.
                    Vous recevrez une notification par email dès qu'une réponse sera disponible.
                </p>
            </div>

            {{ form_start(form) }}
            <div class="form-group">
                {{ form_label(form.titre, null, {'label_attr': {'class': 'required'}}) }}
                {{ form_widget(form.titre, {'attr': {'placeholder': 'Ex: Problème avec ma commande #123'}}) }}
                {{ form_errors(form.titre) }}
            </div>

            <div class="form-group">
                {{ form_label(form.categorie, null, {'label_attr': {'class': 'required'}}) }}
                {{ form_widget(form.categorie, {'attr': {'class': 'form-select'}}) }}
                {{ form_errors(form.categorie) }}
            </div>

            <div class="form-group">
                {{ form_label(form.description, null, {'label_attr': {'class': 'required'}}) }}
                {{ form_widget(form.description, {'attr': {'placeholder': 'Décrivez votre problème en détail...'}}) }}
                {{ form_errors(form.description) }}
            </div>

            {# Les champs nom et email sont pré-remplis par le controller #}
            {{ form_widget(form.nomUtilisateur, {'attr': {'style': 'display:none'}}) }}
            {{ form_widget(form.emailUtilisateur, {'attr': {'style': 'display:none'}}) }}

            {# Champs admin cachés #}
            {{ form_widget(form.statut, {'attr': {'style': 'display:none'}}) }}
            {{ form_widget(form.reponse, {'attr': {'style': 'display:none'}}) }}

            <button type="submit" class="btn-submit">
                📤 Envoyer la réclamation
            </button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
