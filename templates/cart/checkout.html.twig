{% extends 'base.html.twig' %}

{% block title %}Confirmation de Commande - CampingShop{% endblock %}

{% block body %}
<style>
    .checkout-success-container {
        min-height: 60vh;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .success-card {
        background: white;
        border-radius: 30px;
        padding: 4rem 2rem;
        box-shadow: 0 15px 50px rgba(0, 0, 0, 0.1);
        text-align: center;
        max-width: 600px;
        width: 100%;
        border: 1px solid rgba(102, 126, 234, 0.1);
    }

    .success-icon-wrapper {
        width: 100px;
        height: 100px;
        background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 2rem;
        color: white;
        font-size: 3.5rem;
        box-shadow: 0 10px 20px rgba(67, 233, 123, 0.3);
        animation: scaleIn 0.5s ease-out;
    }

    .success-title {
        font-size: 2.5rem;
        font-weight: 850;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 1rem;
    }

    .order-number {
        background: rgba(102, 126, 234, 0.05);
        padding: 0.75rem 1.5rem;
        border-radius: 50px;
        color: #667eea;
        font-weight: 700;
        display: inline-block;
        margin-bottom: 2rem;
    }

    .btn-home {
        padding: 1.25rem 2.5rem;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        border: none;
        border-radius: 50px;
        font-weight: 700;
        text-decoration: none;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        box-shadow: 0 8px 24px rgba(102, 126, 234, 0.3);
    }

    .btn-home:hover {
        transform: translateY(-3px);
        box-shadow: 0 12px 32px rgba(102, 126, 234, 0.4);
        color: white;
    }

    @keyframes scaleIn {
        0% { transform: scale(0); }
        80% { transform: scale(1.1); }
        100% { transform: scale(1); }
    }
</style>

<div class="container checkout-success-container">
    <div class="success-card">
        <div class="success-icon-wrapper">
            <i class="bi bi-check-lg"></i>
        </div>
        
        <h1 class="success-title">Merci pour votre achat !</h1>
        
        <div class="order-number">
            Commande #{{ random(10000, 99999) }}
        </div>

        <p class="text-muted mb-5 px-md-5">
            Votre commande a été validée avec succès. Vous allez recevoir un e-mail de confirmation contenant les détails de votre livraison sous peu.
        </p>

        <div class="d-flex flex-column gap-3">
            <a href="{{ path('app_product_index') }}" class="btn-home">
                <i class="bi bi-bag-check"></i>
                Continuer mes achats
            </a>
            <a href="{{ path('app_home') }}" class="text-decoration-none text-muted fw-bold">
                Retour à l'accueil
            </a>
        </div>
    </div>
</div>
{% endblock %}