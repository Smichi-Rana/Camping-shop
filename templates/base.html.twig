<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Mon App{% endblock %}</title>
</head>
<body>
<nav>
    <a href="{{ path('commande_index') }}">Commandes</a> |
    <a href="{{ path('ligne_index') }}">Lignes de commande</a> |
    <a href="{{ path('facture_index') }}">Factures</a>
</nav>
<hr>

{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="flash-{{ label }}">{{ message }}</div>
    {% endfor %}
{% endfor %}

{% block body %}{% endblock %}
</body>
</html>
